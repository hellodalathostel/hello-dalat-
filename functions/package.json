{
  "name": "hello-dalat-pms-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "echo \"No lint configured\""
  },
  "dependencies": {
    "firebase-admin": "^13.1.0",
    "firebase-functions": "^6.0.1",
    "ical.js": "^1.5.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
